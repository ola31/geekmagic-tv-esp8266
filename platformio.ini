[env:nodemcuv2]
platform = espressif8266
board = nodemcuv2
framework = arduino
board_build.filesystem = littlefs
; This panel's flash needs DOUT: a DIO image boots while warm but crash-loops
; after a cold power cycle. Building DOUT natively means OTA uploads match the
; serial-flashed image, so recovery survives power cycling. Do not change this
; without re-testing the three-power-cycle recovery path on real hardware.
board_build.flash_mode = dout

lib_deps =
    bodmer/TFT_eSPI@^2.5.43
    tzapu/WiFiManager@^2.0.17
    bblanchon/ArduinoJson@^7.0.0
    bodmer/TJpg_Decoder@^1.0.8

build_flags =
    -D PIO_FRAMEWORK_ARDUINO_MMU_CACHE16_IRAM48_SECHEAP_SHARED
    -D USER_SETUP_LOADED=1
    -D SMOOTH_FONT=1
    -D ST7789_DRIVER=1
    -D TFT_WIDTH=240
    -D TFT_HEIGHT=240
    -D TFT_MOSI=13
    -D TFT_SCLK=14
    -D TFT_CS=-1
    -D TFT_DC=0
    -D TFT_RST=2
    -D TFT_BL=5
    -D TOUCH_CS=-1
    -D LOAD_GLCD=1
    -D SPI_FREQUENCY=40000000
    -D SPI_READ_FREQUENCY=20000000


monitor_speed = 115200
; Configure OTA targets per machine in a local override, for example:
;   platformio.local.ini
;   [env:nodemcuv2]
;   upload_protocol = espota
;   upload_port = 192.168.x.x
;   upload_flags = --auth=<current-admin-password>
